{
  "name": "neurocarta-ai-landings",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "npm --prefix neurocarta-conversion run build",
    "deploy:plesk": "cd neurocarta-conversion && npm ci && npm run build && cp -R dist/. .."
  }
}
